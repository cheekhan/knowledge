按照如下改动，修改需求文档和架构设计：
1、FR-3 Markdown 笔记编辑，不需要支持3.5的需求、3.6需求
2、不需要FR-6.2、FR-6.3，PDF文档和笔记建立文件级绑定即可
3、核心场景是打开PDF进行阅读，然后点击创建笔记，就能创建一个和PDF绑定的笔记，PDF和笔记是一对多关系


根据需求设计和架构设计文档，做出一份完整的开发计划，要求：
1、提出需要遵守的公共规范、约束
2、遵循架构设计，保证各个计划直接连续、完整、可执行
3、开发计划详细，需要包括开发内容、开发的细节
4、开发计划间不能互相影响，把一块独立的开发内容尽量放到一个开发计划中
5、生成详细的开发计划文档，我交给其他agent，能够按文档直接实施，执行完计划需要将项目完成 


遵循docs/01-需求设计.md和docs/02-架构设计.md的要求，将以下改动总结为需求，补充到文档中，并且进行开发：
1、修复PDF预览时，点击放大缩小按钮，会导致PDF文件无法渲染
2、PDF预览页面，增加一个折叠右边笔记栏的按钮，能折叠或打开关联的笔记
3、笔记编辑页面，没显示光标


bug修复：
1、aside顶部增加一个折叠按钮，能折叠aside区域，放大main区域
2、阅读PDF时，右侧关联的笔记，也增加一个折叠按钮，能折叠或打开关联的笔记
3、PDF文件，打开时，会自动向下滚动好几页，滚动鼠标时也会突然向下滚动好多页，为我修复这个bug

功能增加：
1、

根据如下任务，在docs/04-优化.md中，生成一个优化方案，要求尽量细致，拆分合理，我会将方案交给其他的agent来执行，要求执行完成后完全符合预期：
1、优化PDF预览的性能，打开大文件时卡顿、白屏
2、没办法展示和添加PDF的书签，PDF标签功能就是书签功能，当前是需求实现错误
3、查看当前开发和运行环境，看看当前技术栈是否为最佳组合，有哪些能优化的地方


---

## ✅ 已完成 — 优化方案 O-01 ~ O-03（详见 `04-优化.md`）

执行日期：2026-07-22

| 优化包 | 内容 | 状态 |
|--------|------|------|
| O-03 | 技术栈与环境评估优化：统一 pnpm 脚本、同步文档版本基线、electron-vite 构建优化（manualChunks）、删除废弃 @types/chokidar、依赖审计 | ✅ |
| O-01 | PDF 预览性能优化：分阶段加载（消除白屏）、懒加载页面尺寸、DOM 窗口化（scrollTop 驱动）、渲染优先级队列、canvas devicePixelRatio 适配 | ✅ |
| O-02 | PDF 书签功能纠正：数据模型迁移（tags→bookmarks，META_VERSION=3）、修复 Outline 页码解析、BookmarkPanel+OutlineTree UI、删除 TagPanel/TagInput/listByTag | ✅ |
| Part D | 文档同步：`01-需求设计.md`、`02-架构设计.md`、`03-开发计划.md` 已更新 | ✅ |
| Part E | 总验收：`pnpm lint && pnpm typecheck` 通过 | ✅ |
